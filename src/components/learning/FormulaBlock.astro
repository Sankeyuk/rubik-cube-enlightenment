---
interface Props {
  formula: string;
}

const { formula } = Astro.props;
---

<section class="formula-block" aria-labelledby="formula-title">
  <span class="eyebrow">核心公式</span>
  <h2 id="formula-title">{formula}</h2>
  <p>先把公式当成一组固定动作。它在不同关卡里影响哪些块，需要后续用图片和展开图确认。</p>
</section>

<style>
  .formula-block {
    display: grid;
    gap: var(--space-sm);
    padding: var(--space-lg);
    border: 1px solid rgba(74, 144, 226, 0.24);
    border-radius: var(--radius-lg);
    background: rgba(74, 144, 226, 0.08);
  }

  .eyebrow {
    color: var(--color-muted-text);
    font-size: var(--font-size-caption);
    line-height: var(--line-height-caption);
  }

  h2 {
    color: var(--color-primary);
  }
</style>
